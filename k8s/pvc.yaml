apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gvault-data
  namespace: gvault
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
